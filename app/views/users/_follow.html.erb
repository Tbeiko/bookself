<%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %>
    <%= hidden_field_tag :@user, @user %></div>
  <%= f.submit :type => :image, :src => "/assets/images/icons/follow.png", class: "follow-button" %>
<% end %>